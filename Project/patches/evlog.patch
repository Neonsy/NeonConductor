diff --git a/dist/logger.mjs b/dist/logger.mjs
index ec025a9..e991b2c 100644
--- a/dist/logger.mjs
+++ b/dist/logger.mjs
@@ -114,6 +114,14 @@ function formatValue(value) {
 	}
 	return String(value);
 }
+function shouldUseAsciiPrettyPrefix() {
+	if (typeof process === "undefined") return false;
+	return process.platform === "win32" && !!process.versions?.electron && process.type !== "renderer";
+}
+function resolvePrettyPrefix(isLast) {
+	if (shouldUseAsciiPrettyPrefix()) return isLast ? "\\-" : "|-";
+	return isLast ? "└─" : "├─";
+}
 function prettyPrintWideEvent(event) {
 	const { timestamp, level, service, environment, version, ...rest } = event;
 	const levelColor = getLevelColor(level);
@@ -138,7 +146,7 @@ function prettyPrintWideEvent(event) {
 	const entries = Object.entries(rest).filter(([_, v]) => v !== void 0);
 	const lastIndex = entries.length - 1;
 	entries.forEach(([key, value], index) => {
-		const prefix = index === lastIndex ? "└─" : "├─";
+		const prefix = resolvePrettyPrefix(index === lastIndex);
 		const formatted = formatValue(value);
 		console.log(`  ${colors.dim}${prefix}${colors.reset} ${colors.cyan}${key}:${colors.reset} ${formatted}`);
 	});
